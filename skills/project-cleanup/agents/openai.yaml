interface:
  display_name: "ProjectCleanup"
  short_description: "Validate clean handoffs for long chats"
  default_prompt: "Use $project-cleanup to check, preview, validate, or prepare a clean handoff for the current project."
